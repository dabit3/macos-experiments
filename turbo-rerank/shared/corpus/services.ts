export type AuthScheme = "api-key" | "oauth" | "mtls" | "hmac";

export interface Endpoint {
  method: "GET" | "POST" | "PUT" | "PATCH" | "DELETE";
  path: string;
  summary: string;
  params: { name: string; type: string; desc: string }[];
  errors: { code: number; name: string; desc: string }[];
}

export interface Alert {
  name: string;
  condition: string;
  cause: string;
  remediation: string;
}

export interface ServiceSpec {
  slug: string;
  name: string;
  team: string;
  purpose: string;
  resource: string;
  auth: AuthScheme;
  rateLimit: number;
  burst: number;
  store: string;
  queue: string | null;
  cache: string | null;
  availability: string;
  latencyMs: number;
  regions: string[];
  language: string;
  endpoints: Endpoint[];
  webhookEvents: string[];
  alerts: Alert[];
  flags: { name: string; def: string; desc: string }[];
  dependencies: string[];
  changelog: { version: string; note: string }[];
  retention: string;
  scaling: string;
}

export const SERVICES: ServiceSpec[] = [
  {
    slug: "ledger-api",
    name: "Ledger API",
    team: "Payments Core",
    purpose: "records double-entry journal entries for every money movement and exposes balances and payouts to merchants",
    resource: "payout",
    auth: "api-key",
    rateLimit: 600,
    burst: 100,
    store: "PostgreSQL 16 (Tier 1, ledger cluster)",
    queue: "Kafka topic ledger.entries",
    cache: null,
    availability: "99.95%",
    latencyMs: 250,
    regions: ["us-east-1", "eu-west-1"],
    language: "Go",
    endpoints: [
      {
        method: "POST",
        path: "/v1/payouts",
        summary: "Creates a payout that moves a merchant's available balance to their bank account",
        params: [
          { name: "amount", type: "integer (minor units)", desc: "amount to pay out, must not exceed the available balance" },
          { name: "currency", type: "ISO 4217 code", desc: "must match the balance currency" },
          { name: "destination", type: "bank account id", desc: "a verified external account belonging to the merchant" },
          { name: "schedule", type: "instant | standard", desc: "instant payouts settle in minutes for a 1% fee, standard settle in 2 business days for free" },
        ],
        errors: [
          { code: 402, name: "insufficient_balance", desc: "the available balance is lower than the requested amount; pending balance cannot be paid out" },
          { code: 409, name: "destination_unverified", desc: "the bank account has not completed micro-deposit verification" },
          { code: 422, name: "currency_mismatch", desc: "the currency does not match the balance being drawn from" },
        ],
      },
      {
        method: "GET",
        path: "/v1/payouts/{id}",
        summary: "Retrieves a payout and its current status (pending, in_transit, paid, failed, canceled)",
        params: [{ name: "expand", type: "string[]", desc: "include `destination` or `balance_transaction` objects inline" }],
        errors: [{ code: 404, name: "payout_not_found", desc: "no payout with that id is visible to the authenticated merchant" }],
      },
      {
        method: "POST",
        path: "/v1/payouts/{id}/cancel",
        summary: "Cancels a payout that has not yet been submitted to the banking network",
        params: [],
        errors: [{ code: 409, name: "payout_already_submitted", desc: "the payout is already in_transit and can no longer be canceled; wait for it to settle or fail" }],
      },
      {
        method: "GET",
        path: "/v1/balance",
        summary: "Returns the available and pending balance per currency",
        params: [{ name: "as_of", type: "RFC 3339 timestamp", desc: "compute the balance as of a past instant (up to 90 days back)" }],
        errors: [],
      },
      {
        method: "GET",
        path: "/v1/balance_transactions",
        summary: "Lists every ledger entry affecting the balance, newest first",
        params: [
          { name: "type", type: "charge | refund | payout | adjustment | fee", desc: "filter by entry type" },
          { name: "created[gte]", type: "timestamp", desc: "only entries created at or after this time" },
        ],
        errors: [],
      },
    ],
    webhookEvents: ["payout.paid", "payout.failed", "payout.canceled", "balance.available"],
    alerts: [
      {
        name: "LedgerPayoutFailureRateHigh",
        condition: "more than 2% of payouts submitted in the last 15 minutes ended in `failed`",
        cause: "usually a banking partner rejecting files, or a bad deploy of the payout submitter",
        remediation: "check the partner status page and the submitter logs for `NACHA_REJECT`; if the partner is down, pause the submitter with the `payouts.submitter.paused` flag so payouts queue instead of failing, and retry the failed batch with `ledgerctl payouts retry --since 1h` once it recovers",
      },
      {
        name: "LedgerEntryLagHigh",
        condition: "consumer lag on `ledger.entries` exceeds 50,000 messages for 5 minutes",
        cause: "a slow PostgreSQL primary, often due to a long-running analytics query holding locks",
        remediation: "run `ledgerctl locks` to list blocking sessions and terminate any analytics session older than 5 minutes; scale the entry consumers to 12 replicas until lag drains",
      },
      {
        name: "LedgerBalanceDrift",
        condition: "the nightly reconciliation finds a merchant whose computed balance differs from the sum of entries",
        cause: "a partially applied journal entry from a crashed writer",
        remediation: "do not adjust the balance by hand; run `ledgerctl reconcile <merchant>` which replays the journal into a shadow table and opens a ticket with the diff for Payments Core to review",
      },
      {
        name: "LedgerApiLatencyP99",
        condition: "p99 latency over 250 ms for 10 minutes",
        cause: "balance queries on merchants with millions of entries missing the covering index after a schema change",
        remediation: "check `pg_stat_statements` for sequential scans on `entries`; if the covering index is missing, recreate it concurrently, and meanwhile enable the `balance.snapshot_reads` flag to serve balances from the hourly snapshot",
      },
      {
        name: "LedgerReplicaLag",
        condition: "read replica is more than 30 seconds behind the primary",
        cause: "heavy write bursts at month-end close",
        remediation: "route balance reads to the primary with the `reads.force_primary` flag while the replica catches up, and page the data platform on-call if lag exceeds 5 minutes",
      },
    ],
    flags: [
      { name: "payouts.submitter.paused", def: "false", desc: "stops submitting payout files to banking partners; payouts stay pending" },
      { name: "balance.snapshot_reads", def: "false", desc: "serve `/v1/balance` from the hourly snapshot instead of summing entries live" },
      { name: "reads.force_primary", def: "false", desc: "send all reads to the primary database" },
    ],
    dependencies: ["auth-gateway", "notify-hub", "the banking partner SFTP gateway"],
    changelog: [
      { version: "2026.08.2", note: "added the `schedule=instant` option for payouts to US bank accounts" },
      { version: "2026.07.1", note: "`as_of` on `/v1/balance` now supports up to 90 days of history instead of 30" },
      { version: "2026.05.3", note: "payout cancellation now returns 409 `payout_already_submitted` instead of a generic 400" },
    ],
    retention: "journal entries are retained forever; payout files exchanged with banks are kept for 7 years",
    scaling: "the API is stateless and scales horizontally; the write path is bounded by the single PostgreSQL primary, which handles about 8,000 entries per second",
  },
  {
    slug: "auth-gateway",
    name: "Auth Gateway",
    team: "Identity",
    purpose: "terminates TLS, validates API keys and OAuth tokens, and forwards authenticated requests to internal services",
    resource: "token",
    auth: "oauth",
    rateLimit: 5000,
    burst: 1000,
    store: "Redis Cluster (session store) and PostgreSQL 16 (clients and keys)",
    queue: null,
    cache: "in-process JWKS cache refreshed every 5 minutes",
    availability: "99.99%",
    latencyMs: 20,
    regions: ["us-east-1", "eu-west-1", "ap-southeast-2"],
    language: "Rust",
    endpoints: [
      {
        method: "POST",
        path: "/oauth/token",
        summary: "Exchanges client credentials or an authorization code for an access token",
        params: [
          { name: "grant_type", type: "client_credentials | authorization_code | refresh_token", desc: "which OAuth 2.1 grant to use" },
          { name: "scope", type: "space-separated string", desc: "requested scopes; the response lists the scopes actually granted" },
          { name: "code_verifier", type: "string", desc: "PKCE verifier, required for authorization_code" },
        ],
        errors: [
          { code: 400, name: "invalid_grant", desc: "the code or refresh token is expired, revoked, or was issued to a different client" },
          { code: 401, name: "invalid_client", desc: "client id or secret is wrong" },
        ],
      },
      {
        method: "POST",
        path: "/oauth/revoke",
        summary: "Revokes an access or refresh token immediately across all regions",
        params: [{ name: "token", type: "string", desc: "the token to revoke; revoking a refresh token also revokes access tokens derived from it" }],
        errors: [],
      },
      {
        method: "POST",
        path: "/oauth/introspect",
        summary: "Returns whether a token is active plus its scopes, client, and expiry",
        params: [{ name: "token", type: "string", desc: "the token to inspect" }],
        errors: [{ code: 403, name: "introspection_forbidden", desc: "only first-party clients may introspect tokens" }],
      },
      {
        method: "GET",
        path: "/v1/api_keys",
        summary: "Lists the API keys of the authenticated organization with their prefixes and last-used time",
        params: [{ name: "include_revoked", type: "boolean", desc: "include revoked keys (default false)" }],
        errors: [],
      },
      {
        method: "POST",
        path: "/v1/api_keys/{id}/roll",
        summary: "Creates a replacement API key and schedules the old key for revocation",
        params: [{ name: "grace_period", type: "duration (max 72h)", desc: "how long the old key keeps working alongside the new one" }],
        errors: [{ code: 409, name: "roll_in_progress", desc: "the key already has a pending roll; revoke or wait for it to finish" }],
      },
    ],
    webhookEvents: ["api_key.rolled", "api_key.revoked", "token.suspicious_use"],
    alerts: [
      {
        name: "AuthGatewayJwksStale",
        condition: "the JWKS cache has not refreshed successfully for 15 minutes",
        cause: "the identity provider's JWKS endpoint is unreachable or returned malformed keys",
        remediation: "the gateway keeps serving with cached keys for up to 24 hours, so this is not yet customer-impacting; check connectivity to the IdP from a gateway pod with `curl -v $JWKS_URL`, and if the IdP rotated keys unexpectedly, run `authctl jwks pin` with the new key set",
      },
      {
        name: "AuthGateway401Spike",
        condition: "401 responses exceed 10% of traffic for 5 minutes",
        cause: "a large customer rolled or revoked their key without updating clients, or a token-signing key was rotated too early",
        remediation: "break down 401s by client id in the auth dashboard; if a single client dominates, notify their account manager; if it is spread across clients, check whether the signing key rotation job ran and roll it back with `authctl keys rollback`",
      },
      {
        name: "AuthGatewayRedisUnavailable",
        condition: "session store commands fail for more than 1% of requests",
        cause: "Redis Cluster failover or a network partition",
        remediation: "the gateway falls back to stateless JWT validation, so API-key traffic continues but browser sessions are logged out; confirm failover completed in the Redis console and scale the cluster if memory is above 80%",
      },
      {
        name: "AuthGatewayLatencyP99",
        condition: "p99 latency above 20 ms for 10 minutes",
        cause: "CPU saturation from a burst of RSA signature verifications, typically a bot storm",
        remediation: "enable the `ratelimit.aggressive` flag to tighten per-IP limits, and scale gateway pods; RSA-signed tokens from legacy clients are the expensive path, so check whether the burst is from a single legacy client",
      },
      {
        name: "AuthGatewayCertificateExpiry",
        condition: "any served TLS certificate expires within 14 days",
        cause: "automated renewal via ACME failed, usually a DNS challenge misconfiguration",
        remediation: "run `authctl certs renew --domain <domain>` and check the ACME logs for the DNS challenge error; if renewal keeps failing, escalate to the platform networking on-call, do not manually upload a certificate",
      },
    ],
    flags: [
      { name: "ratelimit.aggressive", def: "false", desc: "halves per-IP rate limits and blocks IPs with sustained 401s for 10 minutes" },
      { name: "sessions.stateless_fallback", def: "true", desc: "validate JWTs locally when the session store is unavailable" },
    ],
    dependencies: ["the external identity provider", "Redis Cluster", "cert-manager"],
    changelog: [
      { version: "2026.09.1", note: "added `token.suspicious_use` webhook when a token is used from two countries within 5 minutes" },
      { version: "2026.06.2", note: "API key rolling with a grace period replaces the old revoke-and-recreate flow" },
      { version: "2026.04.1", note: "dropped support for the implicit grant; use authorization_code with PKCE" },
    ],
    retention: "access tokens live for 1 hour and refresh tokens for 30 days; audit logs of every token issuance are kept for 7 years",
    scaling: "each gateway pod handles about 20,000 requests per second; pods are added by the autoscaler at 60% CPU",
  },
  {
    slug: "notify-hub",
    name: "Notify Hub",
    team: "Messaging",
    purpose: "delivers transactional email, SMS, and push notifications on behalf of other services, with templates, suppression lists, and delivery tracking",
    resource: "message",
    auth: "api-key",
    rateLimit: 1200,
    burst: 300,
    store: "PostgreSQL 16 (templates, suppressions) and ClickHouse (delivery events)",
    queue: "Kafka topics notify.outbound and notify.events",
    cache: "Redis for template rendering cache",
    availability: "99.9%",
    latencyMs: 400,
    regions: ["us-east-1", "eu-west-1"],
    language: "Kotlin",
    endpoints: [
      {
        method: "POST",
        path: "/v1/messages",
        summary: "Queues a message for delivery over email, sms, or push using a template and variables",
        params: [
          { name: "channel", type: "email | sms | push", desc: "delivery channel" },
          { name: "template", type: "string", desc: "template slug; the template's locale variants are selected from the recipient's profile" },
          { name: "to", type: "recipient object", desc: "email address, E.164 phone number, or device token" },
          { name: "variables", type: "object", desc: "values substituted into the template; missing required variables reject the request" },
          { name: "send_at", type: "timestamp", desc: "schedule delivery up to 30 days in the future" },
        ],
        errors: [
          { code: 422, name: "missing_variable", desc: "the template requires a variable that was not supplied" },
          { code: 409, name: "recipient_suppressed", desc: "the recipient unsubscribed or hard-bounced and cannot be messaged on this channel" },
        ],
      },
      {
        method: "GET",
        path: "/v1/messages/{id}",
        summary: "Returns delivery status: queued, sent, delivered, bounced, complained, or failed, with provider timestamps",
        params: [],
        errors: [{ code: 404, name: "message_not_found", desc: "unknown message id or the message has aged out after 90 days" }],
      },
      {
        method: "POST",
        path: "/v1/suppressions",
        summary: "Adds an address or phone number to the suppression list so no further messages are sent to it",
        params: [
          { name: "address", type: "string", desc: "email or phone" },
          { name: "reason", type: "unsubscribe | bounce | complaint | manual", desc: "why the recipient is suppressed" },
        ],
        errors: [],
      },
      {
        method: "DELETE",
        path: "/v1/suppressions/{address}",
        summary: "Removes a suppression, allowed only for `manual` and `unsubscribe` reasons",
        params: [],
        errors: [{ code: 409, name: "suppression_permanent", desc: "hard bounces and spam complaints cannot be removed from the suppression list by API" }],
      },
      {
        method: "PUT",
        path: "/v1/templates/{slug}",
        summary: "Creates or updates a template with subject, body, and required variables per locale",
        params: [{ name: "locales", type: "map<locale, {subject, body}>", desc: "at least `en` is required; other locales fall back to `en`" }],
        errors: [{ code: 422, name: "template_invalid", desc: "the body references a variable not declared as required or optional" }],
      },
    ],
    webhookEvents: ["message.delivered", "message.bounced", "message.complained", "message.failed"],
    alerts: [
      {
        name: "NotifyHubBounceRateHigh",
        condition: "email bounce rate above 5% over 1 hour",
        cause: "a batch sent to stale addresses, or a sending domain landing on a blocklist",
        remediation: "check the sending domain reputation dashboard; if a single template accounts for the bounces, pause it with `notifyctl template pause <slug>`; if the domain is blocklisted, switch to the warm backup domain with the `email.backup_domain` flag and file a delisting request",
      },
      {
        name: "NotifyHubQueueDepth",
        condition: "more than 200,000 messages queued on `notify.outbound` for 10 minutes",
        cause: "an email provider throttling us, or a service accidentally sending a bulk campaign through the transactional API",
        remediation: "look at the top senders by API key on the queue dashboard; if a single key is flooding, throttle it with `notifyctl key throttle <key> 10/s`; if the provider is throttling, fail over to the secondary provider in the provider console",
      },
      {
        name: "NotifyHubSmsCostSpike",
        condition: "SMS spend in the last hour exceeds 3x the trailing 7-day hourly average",
        cause: "an SMS pumping attack: bots triggering one-time codes to premium-rate numbers",
        remediation: "enable the `sms.geo_block_suspicious` flag, which blocks SMS to the country prefixes on the fraud list, and export the recipient numbers for the security team; do not disable SMS entirely because login codes depend on it",
      },
      {
        name: "NotifyHubTemplateRenderErrors",
        condition: "more than 100 render errors in 5 minutes",
        cause: "a template was updated with a variable that callers do not send",
        remediation: "find the template from the error logs and roll it back with `notifyctl template rollback <slug>`; template versions are kept for 30 days",
      },
    ],
    flags: [
      { name: "email.backup_domain", def: "false", desc: "send from the backup domain instead of the primary" },
      { name: "sms.geo_block_suspicious", def: "false", desc: "block SMS to country prefixes on the fraud list" },
    ],
    dependencies: ["auth-gateway", "two email providers", "one SMS aggregator", "APNs and FCM"],
    changelog: [
      { version: "2026.08.1", note: "scheduled sends via `send_at` up to 30 days ahead" },
      { version: "2026.06.3", note: "templates support per-locale variants with fallback to `en`" },
      { version: "2026.03.2", note: "delivery events moved to ClickHouse; status lookups are now sub-100 ms" },
    ],
    retention: "message bodies are deleted 7 days after delivery; delivery events are kept for 13 months",
    scaling: "outbound workers are partitioned by channel and provider; each email worker sustains about 400 messages per second",
  },
  {
    slug: "search-index",
    name: "Search Index",
    team: "Discovery",
    purpose: "indexes products, orders, and documents from other services and serves full-text and faceted search queries",
    resource: "index",
    auth: "api-key",
    rateLimit: 2000,
    burst: 500,
    store: "OpenSearch 2.x cluster with 9 data nodes",
    queue: "Kafka topic search.updates",
    cache: "Redis query cache with 30-second TTL",
    availability: "99.9%",
    latencyMs: 150,
    regions: ["us-east-1"],
    language: "Java",
    endpoints: [
      {
        method: "POST",
        path: "/v1/indexes/{name}/query",
        summary: "Runs a full-text query with optional facets, filters, and sorting",
        params: [
          { name: "q", type: "string", desc: "query text; supports quoted phrases and `-` for exclusion" },
          { name: "filters", type: "object", desc: "exact-match filters on keyword fields" },
          { name: "facets", type: "string[]", desc: "fields to return counts for" },
          { name: "size", type: "integer (max 100)", desc: "results per page" },
        ],
        errors: [{ code: 422, name: "query_too_complex", desc: "more than 32 clauses or a wildcard on a non-keyword field" }],
      },
      {
        method: "PUT",
        path: "/v1/indexes/{name}/documents/{id}",
        summary: "Upserts a document; changes become searchable within about one second",
        params: [{ name: "body", type: "object", desc: "must conform to the index mapping; unknown fields are rejected" }],
        errors: [{ code: 422, name: "mapping_violation", desc: "a field's type does not match the mapping" }],
      },
      {
        method: "DELETE",
        path: "/v1/indexes/{name}/documents/{id}",
        summary: "Deletes a document from the index",
        params: [],
        errors: [{ code: 404, name: "document_not_found", desc: "the document is not in the index" }],
      },
      {
        method: "POST",
        path: "/v1/indexes/{name}/reindex",
        summary: "Rebuilds the index from the source service into a new physical index and swaps the alias atomically when complete",
        params: [{ name: "source", type: "service name", desc: "which service to pull documents from" }],
        errors: [{ code: 409, name: "reindex_in_progress", desc: "only one reindex per index may run at a time" }],
      },
      {
        method: "GET",
        path: "/v1/indexes/{name}/suggest",
        summary: "Returns type-ahead completions for a prefix",
        params: [{ name: "prefix", type: "string (min 2 chars)", desc: "the text typed so far" }],
        errors: [],
      },
    ],
    webhookEvents: ["reindex.completed", "reindex.failed"],
    alerts: [
      {
        name: "SearchIndexClusterYellow",
        condition: "the OpenSearch cluster health is yellow for more than 15 minutes",
        cause: "a data node restarted and replica shards are re-allocating, or disk watermark reached on a node",
        remediation: "check `_cat/allocation`; if a node is above the 85% disk watermark, delete indexes older than the retention with `searchctl indexes prune`; yellow is not customer-impacting, red is",
      },
      {
        name: "SearchIndexUpdateLag",
        condition: "consumer lag on `search.updates` exceeds 100,000 documents",
        cause: "a bulk backfill from a source service, or indexing throttled by merges",
        remediation: "increase the bulk indexer to 6 replicas and raise `index.refresh_interval` to 30s on the affected index until lag drains, then set it back to 1s",
      },
      {
        name: "SearchIndexQueryLatencyP99",
        condition: "p99 query latency above 150 ms for 10 minutes",
        cause: "an expensive wildcard or deep-pagination query pattern from one client, or a large segment merge",
        remediation: "find the client from the slow-query log; ask them to use `search_after` instead of deep `from`, and if necessary lower their rate limit; check `_nodes/hot_threads` for merge activity",
      },
      {
        name: "SearchIndexReindexFailed",
        condition: "a reindex job failed",
        cause: "the source service returned errors or the mapping changed incompatibly mid-run",
        remediation: "the alias still points at the old index so search is unaffected; read the job log with `searchctl reindex logs <job>`, fix the mapping or source, and re-run; delete the half-built physical index",
      },
    ],
    flags: [
      { name: "query.cache_enabled", def: "true", desc: "cache identical queries for 30 seconds" },
      { name: "suggest.enabled", def: "true", desc: "serve type-ahead suggestions; disable to shed load" },
    ],
    dependencies: ["auth-gateway", "catalog-service", "order-service", "OpenSearch"],
    changelog: [
      { version: "2026.07.3", note: "added `suggest` endpoint for type-ahead" },
      { version: "2026.05.1", note: "reindex now swaps aliases atomically; no more empty results during rebuilds" },
      { version: "2026.02.2", note: "queries with more than 32 clauses are rejected with 422 instead of timing out" },
    ],
    retention: "indexes mirror source data and hold nothing of their own; slow-query logs are kept for 14 days",
    scaling: "add data nodes for storage and query throughput; the cluster rebalances shards automatically, which takes several hours per node",
  },
  {
    slug: "order-service",
    name: "Order Service",
    team: "Commerce",
    purpose: "owns the lifecycle of customer orders from cart checkout through fulfillment, returns, and refunds",
    resource: "order",
    auth: "oauth",
    rateLimit: 900,
    burst: 200,
    store: "PostgreSQL 16 (orders cluster), sharded by merchant",
    queue: "Kafka topic orders.events",
    cache: null,
    availability: "99.95%",
    latencyMs: 300,
    regions: ["us-east-1", "eu-west-1"],
    language: "Go",
    endpoints: [
      {
        method: "POST",
        path: "/v1/orders",
        summary: "Creates an order from a cart, reserving inventory and authorizing payment",
        params: [
          { name: "cart_id", type: "string", desc: "the cart to convert; the cart is frozen on success" },
          { name: "payment_method", type: "string", desc: "a stored payment method id" },
          { name: "shipping_address", type: "address object", desc: "validated against the address service" },
        ],
        errors: [
          { code: 409, name: "inventory_unavailable", desc: "one or more line items are out of stock; the response lists them" },
          { code: 402, name: "payment_declined", desc: "the authorization was declined; the decline code is in `detail`" },
        ],
      },
      {
        method: "POST",
        path: "/v1/orders/{id}/cancel",
        summary: "Cancels an order before it ships, releasing inventory and voiding the payment authorization",
        params: [{ name: "reason", type: "customer_request | fraud | out_of_stock", desc: "recorded on the order" }],
        errors: [{ code: 409, name: "order_already_shipped", desc: "shipped orders cannot be canceled; create a return instead" }],
      },
      {
        method: "POST",
        path: "/v1/orders/{id}/returns",
        summary: "Opens a return for some or all line items of a shipped order and issues a shipping label",
        params: [
          { name: "items", type: "[{line_item_id, quantity, reason}]", desc: "what is being returned" },
          { name: "refund_method", type: "original | store_credit", desc: "store credit adds a 10% bonus" },
        ],
        errors: [{ code: 422, name: "return_window_closed", desc: "the 30-day return window since delivery has passed" }],
      },
      {
        method: "GET",
        path: "/v1/orders",
        summary: "Lists orders for the authenticated customer or merchant with status filters",
        params: [{ name: "status", type: "string[]", desc: "one or more of placed, paid, fulfilling, shipped, delivered, canceled, returned" }],
        errors: [],
      },
      {
        method: "PATCH",
        path: "/v1/orders/{id}/shipping_address",
        summary: "Changes the shipping address while the order is still in placed or paid status",
        params: [{ name: "shipping_address", type: "address object", desc: "the new address" }],
        errors: [{ code: 409, name: "order_in_fulfillment", desc: "the warehouse has started picking; contact support to intercept" }],
      },
    ],
    webhookEvents: ["order.placed", "order.shipped", "order.delivered", "order.canceled", "return.opened", "return.refunded"],
    alerts: [
      {
        name: "OrderServiceCheckoutErrorRate",
        condition: "5xx rate on `POST /v1/orders` above 1% for 5 minutes",
        cause: "the payment provider or inventory service is degraded",
        remediation: "check the dependency panel on the orders dashboard; if payments are failing, enable the `checkout.defer_authorization` flag so orders are placed and authorized asynchronously with retries; do not disable inventory reservation",
      },
      {
        name: "OrderServiceStuckFulfilling",
        condition: "more than 500 orders have been in `fulfilling` for over 48 hours",
        cause: "the warehouse management system stopped sending shipment confirmations",
        remediation: "compare with the WMS export; if shipments exist there, replay confirmations with `orderctl wms replay --since 48h`; otherwise page the fulfillment integration on-call",
      },
      {
        name: "OrderServiceShardHot",
        condition: "one database shard exceeds 80% CPU while others are below 40%",
        cause: "a single very large merchant running a flash sale",
        remediation: "enable read-replica routing for that merchant with `orderctl shard route <merchant> --reads replica`; shard splits are planned work, not an incident action",
      },
      {
        name: "OrderServiceRefundBacklog",
        condition: "more than 1,000 approved refunds not yet issued after 24 hours",
        cause: "the refund worker is failing on ledger-api errors",
        remediation: "check the refund worker logs for `insufficient_balance` from ledger-api, which means the merchant's balance is negative; those refunds wait for the next settlement and should be excluded from the alert with `orderctl refunds mark-waiting`",
      },
    ],
    flags: [
      { name: "checkout.defer_authorization", def: "false", desc: "place orders before payment authorization and authorize asynchronously" },
      { name: "returns.store_credit_bonus", def: "0.10", desc: "bonus fraction added when refunding to store credit" },
    ],
    dependencies: ["auth-gateway", "ledger-api", "inventory-service", "notify-hub", "the warehouse management system"],
    changelog: [
      { version: "2026.09.2", note: "returns can be refunded as store credit with a configurable bonus" },
      { version: "2026.06.1", note: "shipping address changes allowed until fulfillment starts" },
      { version: "2026.04.4", note: "orders sharded by merchant; ids are now prefixed with the shard" },
    ],
    retention: "orders are kept for the life of the customer account plus 90 days; financial summaries are archived for 7 years",
    scaling: "horizontal on the API tier; database capacity grows by splitting shards, which is a scheduled operation taking about 4 hours",
  },
  {
    slug: "inventory-service",
    name: "Inventory Service",
    team: "Commerce",
    purpose: "tracks stock levels per SKU per warehouse, handles reservations during checkout, and reconciles with warehouse counts",
    resource: "reservation",
    auth: "mtls",
    rateLimit: 3000,
    burst: 600,
    store: "PostgreSQL 16 with per-warehouse partitions",
    queue: "Kafka topic inventory.movements",
    cache: "Redis for hot SKU counts",
    availability: "99.95%",
    latencyMs: 80,
    regions: ["us-east-1", "eu-west-1"],
    language: "Go",
    endpoints: [
      {
        method: "POST",
        path: "/v1/reservations",
        summary: "Reserves quantities of SKUs for a cart for 15 minutes",
        params: [
          { name: "items", type: "[{sku, quantity}]", desc: "what to reserve" },
          { name: "warehouse", type: "string", desc: "optional; otherwise the nearest warehouse with stock is chosen" },
        ],
        errors: [{ code: 409, name: "insufficient_stock", desc: "the response lists each SKU with the quantity available" }],
      },
      {
        method: "POST",
        path: "/v1/reservations/{id}/commit",
        summary: "Converts a reservation into a stock deduction when the order is placed",
        params: [],
        errors: [{ code: 410, name: "reservation_expired", desc: "the 15-minute hold elapsed; create a new reservation" }],
      },
      {
        method: "DELETE",
        path: "/v1/reservations/{id}",
        summary: "Releases a reservation early",
        params: [],
        errors: [],
      },
      {
        method: "GET",
        path: "/v1/stock/{sku}",
        summary: "Returns on-hand, reserved, and available quantities per warehouse for a SKU",
        params: [],
        errors: [{ code: 404, name: "sku_unknown", desc: "the SKU is not in the catalog" }],
      },
      {
        method: "POST",
        path: "/v1/adjustments",
        summary: "Records a manual stock adjustment from a warehouse count, with a reason code",
        params: [
          { name: "sku", type: "string", desc: "SKU to adjust" },
          { name: "delta", type: "integer", desc: "positive or negative change" },
          { name: "reason", type: "count | damage | shrinkage | return", desc: "why the adjustment was made" },
        ],
        errors: [{ code: 403, name: "adjustment_forbidden", desc: "only warehouse-role certificates may post adjustments" }],
      },
    ],
    webhookEvents: ["stock.low", "stock.out", "reservation.expired"],
    alerts: [
      {
        name: "InventoryOversell",
        condition: "any SKU has negative available stock",
        cause: "a reservation was committed twice, or a warehouse count adjustment arrived after orders were placed",
        remediation: "run `invctl audit <sku>` to list the movements that drove it negative; oversold orders are flagged to the order service automatically, so the action here is to correct the count and notify the Commerce team, not to cancel orders",
      },
      {
        name: "InventoryRedisMiss",
        condition: "hot SKU cache hit rate below 90%",
        cause: "Redis was flushed or a large catalog import created many new SKUs",
        remediation: "warm the cache with `invctl cache warm --top 10000`; the service reads through to PostgreSQL on a miss so this is a latency problem, not a correctness problem",
      },
      {
        name: "InventoryReservationLeak",
        condition: "more than 50,000 active reservations older than 20 minutes",
        cause: "the expiry sweeper stopped running",
        remediation: "check the sweeper cron job; restart it with `kubectl rollout restart cronjob/reservation-sweeper` and verify expired reservations drain within 10 minutes",
      },
      {
        name: "InventoryMovementLag",
        condition: "consumer lag on `inventory.movements` above 20,000",
        cause: "a warehouse bulk receipt",
        remediation: "scale movement consumers to 8 replicas; lag under an hour is acceptable because warehouse receipts are not time-critical",
      },
    ],
    flags: [
      { name: "reservations.ttl_minutes", def: "15", desc: "how long a reservation holds stock" },
      { name: "stock.allow_backorder", def: "false", desc: "let reservations succeed against incoming purchase orders" },
    ],
    dependencies: ["order-service", "catalog-service", "the warehouse management system"],
    changelog: [
      { version: "2026.08.3", note: "reservations pick the nearest warehouse with stock when none is specified" },
      { version: "2026.05.2", note: "mTLS replaces API keys for all callers; warehouse role required for adjustments" },
      { version: "2026.03.1", note: "added `stock.low` webhook with per-SKU thresholds" },
    ],
    retention: "stock movements are kept for 3 years for audit; reservations are deleted 24 hours after they expire",
    scaling: "partitioned by warehouse; a new warehouse gets its own partition and the hot-SKU cache is per region",
  },
  {
    slug: "catalog-service",
    name: "Catalog Service",
    team: "Commerce",
    purpose: "stores products, variants, prices, and media, and publishes catalog changes to search and storefronts",
    resource: "product",
    auth: "oauth",
    rateLimit: 1500,
    burst: 300,
    store: "PostgreSQL 16 and S3 for media",
    queue: "Kafka topic catalog.changes",
    cache: "CDN for media and a Redis read cache for product pages",
    availability: "99.9%",
    latencyMs: 120,
    regions: ["us-east-1", "eu-west-1"],
    language: "TypeScript (Node 22)",
    endpoints: [
      {
        method: "POST",
        path: "/v1/products",
        summary: "Creates a product with variants, attributes, and initial prices",
        params: [
          { name: "title", type: "string", desc: "display name" },
          { name: "variants", type: "[{sku, options, price}]", desc: "at least one variant; SKUs must be globally unique" },
          { name: "status", type: "draft | active | archived", desc: "drafts are invisible to storefronts" },
        ],
        errors: [{ code: 409, name: "sku_taken", desc: "a variant SKU already exists on another product" }],
      },
      {
        method: "PATCH",
        path: "/v1/products/{id}",
        summary: "Updates product fields; changes are published to storefronts within a few seconds",
        params: [{ name: "fields", type: "partial product", desc: "only provided fields change" }],
        errors: [{ code: 409, name: "version_conflict", desc: "the `If-Match` version header is stale; refetch and retry" }],
      },
      {
        method: "POST",
        path: "/v1/products/{id}/media",
        summary: "Uploads an image or video; returns CDN URLs for each generated rendition",
        params: [{ name: "file", type: "multipart (max 25 MB)", desc: "JPEG, PNG, WebP, or MP4" }],
        errors: [{ code: 413, name: "media_too_large", desc: "over 25 MB; compress before upload" }],
      },
      {
        method: "PUT",
        path: "/v1/prices/{sku}",
        summary: "Sets the price for a SKU per currency with optional scheduled start and end",
        params: [
          { name: "amount", type: "integer (minor units)", desc: "price" },
          { name: "starts_at", type: "timestamp", desc: "when the price takes effect; omitted means now" },
        ],
        errors: [{ code: 422, name: "price_below_cost", desc: "the price is below the recorded unit cost and the merchant has cost protection enabled" }],
      },
      {
        method: "POST",
        path: "/v1/imports",
        summary: "Imports a CSV of products asynchronously and reports row-level errors",
        params: [{ name: "file", type: "CSV (max 100,000 rows)", desc: "using the documented column layout" }],
        errors: [{ code: 422, name: "csv_invalid", desc: "header row does not match the expected columns" }],
      },
    ],
    webhookEvents: ["product.created", "product.updated", "product.archived", "price.changed", "import.completed"],
    alerts: [
      {
        name: "CatalogPublishLag",
        condition: "median time from product update to storefront visibility above 30 seconds",
        cause: "the change publisher is behind, typically after a large CSV import",
        remediation: "check `catalog.changes` lag; if an import is the cause, throttle it with `catctl imports throttle <id> 100/s` rather than stopping it",
      },
      {
        name: "CatalogMediaProcessingFailed",
        condition: "more than 5% of media uploads fail rendition generation in 15 minutes",
        cause: "the image worker's libvips update or a burst of HEIC files, which are not supported",
        remediation: "inspect worker logs; roll back the worker deploy if the failures started with it; HEIC uploads should be rejected at the API with 415, so if they are reaching the worker, check the content-type validation",
      },
      {
        name: "CatalogCdnErrorRate",
        condition: "CDN 5xx above 0.5% for 10 minutes",
        cause: "origin S3 bucket throttling or a CDN provider incident",
        remediation: "check the CDN provider status page; if S3 is throttling, enable `media.origin_shield` to add a caching layer in front of the bucket",
      },
    ],
    flags: [
      { name: "media.origin_shield", def: "false", desc: "route CDN misses through the origin shield cache" },
      { name: "imports.max_rows", def: "100000", desc: "maximum rows per CSV import" },
    ],
    dependencies: ["auth-gateway", "search-index", "S3", "the CDN"],
    changelog: [
      { version: "2026.09.1", note: "scheduled prices with `starts_at` and `ends_at`" },
      { version: "2026.07.2", note: "optimistic concurrency via `If-Match` on product updates" },
      { version: "2026.04.2", note: "video media supported up to 25 MB" },
    ],
    retention: "archived products and their media are kept for 2 years and then deleted; import files are deleted after 30 days",
    scaling: "stateless API; media processing workers autoscale on queue depth",
  },
  {
    slug: "billing-engine",
    name: "Billing Engine",
    team: "Monetization",
    purpose: "meters usage, rates it against price plans, and generates invoices and subscription renewals for platform customers",
    resource: "invoice",
    auth: "api-key",
    rateLimit: 800,
    burst: 200,
    store: "PostgreSQL 16 (Tier 1, billing cluster) and ClickHouse for raw usage",
    queue: "Kafka topic billing.usage",
    cache: null,
    availability: "99.9%",
    latencyMs: 500,
    regions: ["us-east-1"],
    language: "Python 3.12",
    endpoints: [
      {
        method: "POST",
        path: "/v1/usage",
        summary: "Records usage events for metered billing; events are deduplicated by `event_id` for 7 days",
        params: [
          { name: "event_id", type: "UUID", desc: "client-generated id for deduplication" },
          { name: "meter", type: "string", desc: "the meter slug, e.g. `api_calls` or `gb_stored`" },
          { name: "quantity", type: "decimal", desc: "amount of usage" },
          { name: "timestamp", type: "timestamp", desc: "when the usage occurred; must be within the last 72 hours" },
        ],
        errors: [{ code: 422, name: "usage_too_old", desc: "the timestamp is more than 72 hours in the past and the billing period may already be closed" }],
      },
      {
        method: "GET",
        path: "/v1/invoices/{id}",
        summary: "Retrieves an invoice with line items, taxes, and payment status",
        params: [{ name: "format", type: "json | pdf", desc: "PDF returns a signed URL valid for 1 hour" }],
        errors: [{ code: 404, name: "invoice_not_found", desc: "unknown invoice id" }],
      },
      {
        method: "POST",
        path: "/v1/invoices/{id}/void",
        summary: "Voids an open invoice; paid invoices must be credited instead",
        params: [{ name: "reason", type: "string", desc: "shown on the customer's billing history" }],
        errors: [{ code: 409, name: "invoice_paid", desc: "issue a credit note with `POST /v1/credit_notes` instead" }],
      },
      {
        method: "POST",
        path: "/v1/subscriptions",
        summary: "Starts a subscription to a plan with an optional trial and proration",
        params: [
          { name: "plan", type: "string", desc: "plan slug" },
          { name: "trial_days", type: "integer (max 30)", desc: "free trial length" },
          { name: "billing_anchor", type: "day of month", desc: "when renewals bill; defaults to the start date" },
        ],
        errors: [{ code: 409, name: "subscription_exists", desc: "the customer already has an active subscription to this plan family; use change instead" }],
      },
      {
        method: "POST",
        path: "/v1/subscriptions/{id}/change",
        summary: "Upgrades or downgrades a subscription with proration on the next invoice",
        params: [{ name: "plan", type: "string", desc: "the new plan" }, { name: "effective", type: "now | period_end", desc: "when the change applies; downgrades default to period_end" }],
        errors: [],
      },
    ],
    webhookEvents: ["invoice.finalized", "invoice.paid", "invoice.payment_failed", "subscription.renewed", "subscription.canceled"],
    alerts: [
      {
        name: "BillingInvoiceRunFailed",
        condition: "the nightly invoice run did not complete by 06:00 UTC",
        cause: "a rating error on a plan with a misconfigured tier, or ClickHouse timeouts aggregating usage",
        remediation: "check the run log with `billctl runs latest`; a single failing customer can be skipped with `billctl runs skip <customer>` so the rest of the run completes, then fix the plan and invoice the skipped customer manually",
      },
      {
        name: "BillingUsageDropoff",
        condition: "usage events received in the last hour are below 30% of the same hour last week",
        cause: "a producing service stopped emitting usage, which would under-bill customers",
        remediation: "break down by meter on the usage dashboard to find the missing producer and page its on-call; usage can be backfilled within 72 hours, after that finance must approve a correction invoice",
      },
      {
        name: "BillingPaymentFailureRate",
        condition: "more than 15% of renewal payments failed in the last 6 hours",
        cause: "the card processor is declining, or a bug in the retry schedule",
        remediation: "check the processor's status; renewals retry on days 1, 3, and 7 automatically, so do not manually retry; if the processor is down, extend the dunning grace period with the `dunning.grace_days` flag",
      },
      {
        name: "BillingTaxServiceErrors",
        condition: "tax calculation errors above 1% of invoices",
        cause: "the tax provider API is down or a customer address failed validation",
        remediation: "invoices with tax errors are held, not sent; retry with `billctl invoices retry-tax` once the provider recovers; addresses that fail validation are queued for the billing ops team",
      },
    ],
    flags: [
      { name: "dunning.grace_days", def: "7", desc: "days after a failed renewal before access is suspended" },
      { name: "usage.late_event_hours", def: "72", desc: "how old a usage event may be and still be accepted" },
    ],
    dependencies: ["auth-gateway", "ledger-api", "notify-hub", "the card processor", "the tax provider"],
    changelog: [
      { version: "2026.08.2", note: "subscription changes can take effect at period end with proration" },
      { version: "2026.06.1", note: "invoice PDFs via `format=pdf` with signed URLs" },
      { version: "2026.02.3", note: "usage events accepted up to 72 hours late instead of 24" },
    ],
    retention: "invoices are kept for 10 years; raw usage events are kept for 13 months and monthly aggregates forever",
    scaling: "the nightly invoice run is partitioned by customer id into 32 workers; ClickHouse handles about 2 million usage events per second",
  },
  {
    slug: "webhook-relay",
    name: "Webhook Relay",
    team: "Platform Integrations",
    purpose: "delivers webhook events from internal services to customer endpoints with signing, retries, and a replay UI",
    resource: "delivery",
    auth: "hmac",
    rateLimit: 10000,
    burst: 2000,
    store: "PostgreSQL 16 for endpoints and DynamoDB for delivery attempts",
    queue: "SQS queues per retry tier",
    cache: null,
    availability: "99.9%",
    latencyMs: 2000,
    regions: ["us-east-1", "eu-west-1"],
    language: "Go",
    endpoints: [
      {
        method: "POST",
        path: "/v1/endpoints",
        summary: "Registers a customer webhook endpoint URL with the event types it subscribes to",
        params: [
          { name: "url", type: "https URL", desc: "must be HTTPS and publicly resolvable; private IP ranges are rejected" },
          { name: "events", type: "string[]", desc: "event types, wildcards like `order.*` allowed" },
        ],
        errors: [{ code: 422, name: "url_not_https", desc: "plain HTTP endpoints are not accepted" }, { code: 422, name: "url_private_range", desc: "the URL resolves to a private or loopback address" }],
      },
      {
        method: "POST",
        path: "/v1/endpoints/{id}/rotate_secret",
        summary: "Issues a new signing secret; the old secret remains valid for 24 hours",
        params: [],
        errors: [],
      },
      {
        method: "GET",
        path: "/v1/deliveries",
        summary: "Lists delivery attempts for an endpoint with status codes and response snippets",
        params: [
          { name: "endpoint", type: "string", desc: "endpoint id" },
          { name: "status", type: "succeeded | failed | pending", desc: "filter" },
        ],
        errors: [],
      },
      {
        method: "POST",
        path: "/v1/deliveries/{id}/replay",
        summary: "Re-sends a specific event to its endpoint immediately, outside the retry schedule",
        params: [],
        errors: [{ code: 409, name: "delivery_pending", desc: "a retry is already scheduled" }],
      },
      {
        method: "POST",
        path: "/v1/endpoints/{id}/test",
        summary: "Sends a synthetic `ping` event and returns the endpoint's response",
        params: [],
        errors: [],
      },
    ],
    webhookEvents: ["endpoint.disabled", "endpoint.recovered"],
    alerts: [
      {
        name: "WebhookRelayRetryQueueDepth",
        condition: "more than 1 million deliveries in retry queues",
        cause: "a large customer's endpoint is down and every event to it is retrying",
        remediation: "find the endpoint from the queue breakdown; an endpoint failing for 24 hours is disabled automatically and its customer emailed, so usually no action is needed; if a single endpoint holds more than 30% of the queue, disable it early with `relayctl endpoint disable <id>`",
      },
      {
        name: "WebhookRelayDeliveryLatencyP95",
        condition: "p95 time from event to first delivery attempt above 2 seconds",
        cause: "the first-attempt workers are saturated",
        remediation: "scale the `tier0` worker deployment; retry tiers have separate workers so first attempts should never wait behind retries — if they do, check that the tier routing config was not changed",
      },
      {
        name: "WebhookRelaySsrfBlocked",
        condition: "more than 100 deliveries blocked by the private-range check in an hour",
        cause: "a customer's DNS started resolving to a private address, or a deliberate SSRF probe",
        remediation: "list the endpoints with `relayctl ssrf-blocked --since 1h`; notify the security team if the URLs look like probes; customers whose DNS changed get an automatic notification",
      },
    ],
    flags: [
      { name: "delivery.timeout_seconds", def: "10", desc: "how long to wait for a customer endpoint to respond" },
      { name: "endpoint.auto_disable_hours", def: "24", desc: "disable an endpoint after this many hours of continuous failure" },
    ],
    dependencies: ["all event-producing services", "SQS", "DynamoDB"],
    changelog: [
      { version: "2026.07.1", note: "signing secret rotation with 24-hour overlap" },
      { version: "2026.05.4", note: "replay UI shows response bodies truncated to 1 KB" },
      { version: "2026.03.3", note: "retry schedule extended to 24 hours with exponential backoff" },
    ],
    retention: "delivery attempts including response snippets are kept for 30 days; endpoint configuration indefinitely",
    scaling: "workers per retry tier autoscale on queue depth; DynamoDB is on-demand capacity",
  },
  {
    slug: "feature-flags",
    name: "Flagship",
    team: "Developer Platform",
    purpose: "serves feature flag evaluations to every service and client SDK, with targeting rules and gradual rollouts",
    resource: "flag",
    auth: "api-key",
    rateLimit: 20000,
    burst: 5000,
    store: "PostgreSQL 16 for definitions; flag snapshots pushed to an edge KV store",
    queue: null,
    cache: "SDKs cache the full ruleset and receive updates over a streaming connection",
    availability: "99.99%",
    latencyMs: 5,
    regions: ["us-east-1", "eu-west-1", "ap-southeast-2"],
    language: "Go",
    endpoints: [
      {
        method: "GET",
        path: "/v1/evaluate",
        summary: "Evaluates all flags for a context (user, organization, environment) and returns their values",
        params: [{ name: "context", type: "object", desc: "attributes used by targeting rules; must include `key`" }],
        errors: [],
      },
      {
        method: "PUT",
        path: "/v1/flags/{key}",
        summary: "Creates or updates a flag definition with variations and default rules",
        params: [
          { name: "variations", type: "array", desc: "boolean, string, number, or JSON values" },
          { name: "tags", type: "string[]", desc: "e.g. `permanent` to exempt from the stale flag linter" },
        ],
        errors: [{ code: 409, name: "flag_archived", desc: "archived flags cannot be edited; unarchive first" }],
      },
      {
        method: "POST",
        path: "/v1/flags/{key}/rollout",
        summary: "Sets the percentage rollout for an environment, sticky by context key",
        params: [
          { name: "environment", type: "string", desc: "staging or production" },
          { name: "percentage", type: "0-100", desc: "share of contexts receiving the variation" },
        ],
        errors: [{ code: 422, name: "rollout_step_too_large", desc: "production rollouts may increase by at most 25 points per change unless the flag is tagged `ops`" }],
      },
      {
        method: "POST",
        path: "/v1/flags/{key}/kill",
        summary: "Immediately serves the flag's off variation to everyone in an environment, bypassing all rules",
        params: [{ name: "environment", type: "string", desc: "which environment" }],
        errors: [],
      },
      {
        method: "GET",
        path: "/v1/flags/{key}/history",
        summary: "Returns every change to a flag with who made it and when",
        params: [],
        errors: [],
      },
    ],
    webhookEvents: ["flag.changed", "flag.killed", "flag.archived"],
    alerts: [
      {
        name: "FlagshipStreamDisconnects",
        condition: "more than 20% of SDK streaming connections dropped in 5 minutes",
        cause: "an edge node restart or a load balancer idle timeout change",
        remediation: "SDKs keep serving their cached ruleset, so evaluations continue; check the edge deployment status and that the load balancer idle timeout is still 300 seconds",
      },
      {
        name: "FlagshipSnapshotStale",
        condition: "the edge KV snapshot is more than 60 seconds older than the latest flag change",
        cause: "the snapshot publisher failed",
        remediation: "restart the publisher; if a kill switch was just flipped, verify it reached the edge with `flagctl verify <key> --env production` because kill switches must propagate within 10 seconds",
      },
      {
        name: "FlagshipEvaluationLatency",
        condition: "server-side evaluation p99 above 5 ms",
        cause: "a flag with thousands of individually targeted keys",
        remediation: "find the flag with `flagctl rules largest`; ask the owner to move individual targets into a segment, which is evaluated as a set",
      },
    ],
    flags: [
      { name: "rollout.max_step_production", def: "25", desc: "maximum percentage increase per production rollout change" },
      { name: "stale.days", def: "30", desc: "days at 100% before a flag is considered stale" },
    ],
    dependencies: ["auth-gateway", "the edge KV store"],
    changelog: [
      { version: "2026.09.3", note: "kill switches propagate to edge within 10 seconds via a priority channel" },
      { version: "2026.06.4", note: "flag history endpoint records the actor for every change" },
      { version: "2026.04.3", note: "production rollout steps capped at 25 points" },
    ],
    retention: "flag change history is kept forever; evaluation logs are sampled at 1% and kept for 30 days",
    scaling: "evaluation is served from the edge and SDK caches; the control plane handles only writes",
  },
  {
    slug: "media-transcoder",
    name: "Media Transcoder",
    team: "Media Platform",
    purpose: "transcodes uploaded video and audio into adaptive streaming renditions and generates thumbnails and captions",
    resource: "job",
    auth: "oauth",
    rateLimit: 300,
    burst: 50,
    store: "PostgreSQL 16 for jobs and S3 for inputs and outputs",
    queue: "SQS with a GPU and a CPU queue",
    cache: null,
    availability: "99.5%",
    latencyMs: 60000,
    regions: ["us-east-1"],
    language: "Python 3.12 and ffmpeg",
    endpoints: [
      {
        method: "POST",
        path: "/v1/jobs",
        summary: "Submits a transcode job for an object in S3 with a preset",
        params: [
          { name: "input", type: "s3 URL", desc: "source object; must be in a bucket the transcoder role can read" },
          { name: "preset", type: "hls_1080p | hls_4k | audio_only | thumbnail_sheet", desc: "output profile" },
          { name: "captions", type: "boolean", desc: "generate captions with speech recognition" },
        ],
        errors: [{ code: 422, name: "input_unreadable", desc: "the object does not exist or the bucket policy denies the transcoder role" }],
      },
      {
        method: "GET",
        path: "/v1/jobs/{id}",
        summary: "Returns job state (queued, running, complete, failed) with progress percentage and output URLs",
        params: [],
        errors: [{ code: 404, name: "job_not_found", desc: "unknown job id" }],
      },
      {
        method: "POST",
        path: "/v1/jobs/{id}/cancel",
        summary: "Cancels a queued or running job; partial outputs are deleted",
        params: [],
        errors: [{ code: 409, name: "job_complete", desc: "completed jobs cannot be canceled" }],
      },
      {
        method: "GET",
        path: "/v1/presets",
        summary: "Lists available presets with their renditions, codecs, and bitrates",
        params: [],
        errors: [],
      },
    ],
    webhookEvents: ["job.complete", "job.failed", "job.progress"],
    alerts: [
      {
        name: "TranscoderGpuQueueWait",
        condition: "median time in the GPU queue above 10 minutes",
        cause: "GPU spot capacity was reclaimed, or a batch of 4K jobs",
        remediation: "check the GPU node group; if spot capacity is unavailable, enable the `queue.gpu_fallback_cpu` flag so 1080p jobs run on CPU nodes (about 4x slower) while 4K jobs wait",
      },
      {
        name: "TranscoderJobFailureRate",
        condition: "more than 5% of jobs failed in the last hour",
        cause: "a corrupt or unusual input codec, or an ffmpeg upgrade",
        remediation: "group failures by `ffmpeg_exit_code` in the job dashboard; if they cluster after a deploy, roll back; if they are a single customer's uploads, the input is probably variable-frame-rate or has a broken moov atom and the customer should be told to remux",
      },
      {
        name: "TranscoderStorageCost",
        condition: "output bucket size grew more than 20% in a week",
        cause: "a customer generating many renditions or the lifecycle rule for deleted jobs not running",
        remediation: "check that the S3 lifecycle rule deleting outputs of canceled jobs is active and look at the top uploaders by bytes",
      },
    ],
    flags: [
      { name: "queue.gpu_fallback_cpu", def: "false", desc: "run 1080p jobs on CPU nodes when GPU capacity is unavailable" },
      { name: "captions.enabled", def: "true", desc: "allow caption generation" },
    ],
    dependencies: ["auth-gateway", "S3", "the speech recognition provider", "GPU spot node group"],
    changelog: [
      { version: "2026.08.4", note: "caption generation via `captions=true`" },
      { version: "2026.05.1", note: "added `hls_4k` preset" },
      { version: "2026.02.1", note: "progress webhooks every 10%" },
    ],
    retention: "inputs are deleted 7 days after job completion; outputs belong to the customer's bucket and are not managed by the transcoder",
    scaling: "workers scale on queue depth; GPU workers use spot instances with an on-demand floor of 2",
  },
  {
    slug: "audit-log",
    name: "Audit Log",
    team: "Security Engineering",
    purpose: "collects immutable audit events from every service and admin tool and exposes them for compliance queries and customer export",
    resource: "event",
    auth: "mtls",
    rateLimit: 50000,
    burst: 10000,
    store: "append-only object storage with a ClickHouse query layer",
    queue: "Kafka topic audit.events with 3 replicas and acks=all",
    cache: null,
    availability: "99.9%",
    latencyMs: 1000,
    regions: ["us-east-1", "eu-west-1"],
    language: "Rust",
    endpoints: [
      {
        method: "POST",
        path: "/v1/events",
        summary: "Appends audit events; each event is hashed and chained to the previous event of the same tenant",
        params: [
          { name: "actor", type: "object", desc: "who did it: user id or service identity" },
          { name: "action", type: "string", desc: "dotted verb like `api_key.revoke`" },
          { name: "target", type: "object", desc: "what it was done to" },
          { name: "metadata", type: "object", desc: "free-form; must not contain secrets or card numbers, the ingest filter rejects known patterns" },
        ],
        errors: [{ code: 422, name: "metadata_contains_secret", desc: "the metadata matched a secret or card number pattern and was rejected" }],
      },
      {
        method: "POST",
        path: "/v1/query",
        summary: "Runs a filtered query over events by actor, action, target, and time range",
        params: [
          { name: "tenant", type: "string", desc: "tenant id; cross-tenant queries need the compliance role" },
          { name: "range", type: "{from, to}", desc: "up to 1 year per query" },
        ],
        errors: [{ code: 403, name: "cross_tenant_forbidden", desc: "the caller's certificate lacks the compliance role" }],
      },
      {
        method: "POST",
        path: "/v1/exports",
        summary: "Starts an asynchronous export of a tenant's events to a signed download in CSV or JSON Lines",
        params: [{ name: "format", type: "csv | jsonl", desc: "output format" }],
        errors: [],
      },
      {
        method: "GET",
        path: "/v1/verify/{tenant}",
        summary: "Verifies the hash chain for a tenant over a time range and reports any break",
        params: [],
        errors: [],
      },
    ],
    webhookEvents: ["export.ready", "chain.break_detected"],
    alerts: [
      {
        name: "AuditLogChainBreak",
        condition: "the hourly verifier found a hash chain break",
        cause: "an out-of-order write after a Kafka rebalance, or tampering",
        remediation: "treat as a SEV2 security incident until proven otherwise: page security on-call, run `auditctl verify --tenant <t> --verbose` to locate the break, and do not attempt to repair the chain — the verifier's report is itself evidence",
      },
      {
        name: "AuditLogIngestLag",
        condition: "consumer lag on `audit.events` above 1 million events",
        cause: "ClickHouse insert throttling",
        remediation: "events are durable in Kafka for 7 days so nothing is lost; scale the inserter to 12 replicas and check ClickHouse merge backlog",
      },
      {
        name: "AuditLogRejectedSecrets",
        condition: "more than 50 events rejected for containing secrets in an hour",
        cause: "a service is logging a credential into audit metadata",
        remediation: "find the producer from the rejection log and page its on-call; the credential must be rotated as if leaked, following the secrets leak procedure",
      },
    ],
    flags: [
      { name: "ingest.reject_secrets", def: "true", desc: "reject events whose metadata matches secret patterns" },
      { name: "query.max_range_days", def: "365", desc: "maximum time range per query" },
    ],
    dependencies: ["Kafka", "ClickHouse", "object storage with object lock"],
    changelog: [
      { version: "2026.09.2", note: "chain verification endpoint and `chain.break_detected` webhook" },
      { version: "2026.06.2", note: "customer self-service exports in CSV and JSON Lines" },
      { version: "2026.03.4", note: "ingest filter rejects metadata containing card numbers or key formats" },
    ],
    retention: "events are kept for 7 years in object storage with object lock; the ClickHouse query layer holds the most recent 13 months",
    scaling: "ingest scales with Kafka partitions (currently 96); queries are limited by ClickHouse capacity and are rate limited per tenant",
  },
];
